# Mount target is already created in the compartment DBE compartment




# module "mount_target" {
#   source = "../../modules/storage/mount_target"

#   env_name              = var.env_name
#   compartment_id        = "var.compartment_id"
#   root_compartment_id   = var.root_compartment_id
#   private_subnet_id     = module.private_subnet.private_subnet_id
#providers = {
#    oci = oci
#  }
  
# }